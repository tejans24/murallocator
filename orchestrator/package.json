{
  "name": "lookup-server",
  "version": "0.0.1",
  "private": true,
  "babel": {
    "presets": [
      "es2015",
      "stage-0"
    ]
  },
  "dependencies": {
    "chokidar": "^1.6.1",
    "co-mocha": "^1.2.0",
    "express": "4.13.3",
    "express-http-proxy": "^0.11.0",
    "fs": "0.0.2",
    "http-proxy": "^1.16.2",
    "passport": "^0.3.2",
    "request": "^2.79.0"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "chai-http": "^3.0.0",
    "http-proxy-middleware": "^0.17.3",
    "mocha": "^3.2.0",
    "sinon": "^1.17.7",
    "sinon-chai": "^2.8.0"
  },
  "scripts": {
    "test": "yarn run test-unit",
    "test-unit": "NODE_ENV=test mocha --require co-mocha --reporter spec --timeout 10000"
  }
}
